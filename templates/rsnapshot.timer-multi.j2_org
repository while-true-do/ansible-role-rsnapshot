[Unit]
Description=rsnapshot "{{ item.0.name }}"  backup

[Timer]
# 14:30 is the clock time when to start it
OnCalendar={{ item.1.time | default(wtd_rsnapshot_timer_time) }}
Persistent=true
Unit=rsnapshot@{{ item.1.name }}.service

[Install]
WantedBy=timers.target
